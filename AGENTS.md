# AGENTS.md

本文档用于约束本项目中的 AI / 自动化开发行为。开发时优先遵循本文件，其次遵循用户当前消息。

## 基本原则

- 先读现有代码，再动手修改，优先沿用项目已有结构和写法。
- 用户用截图指出界面问题后，先固定问题结论、目标状态和验收条件；同一截图默认只查看一次，后续基于源码与浏览器结果推进。只有修复结果与目标不符或用户提供新截图时才重新查看，禁止在没有新证据时反复看图并重复做同类局部调整。
- 继续或恢复已有任务时，必须先核对任务清单、当前差异和已有测试/浏览器证据；已标记完成且后续修改未使证据失效的步骤不得重复执行或重复汇报。只有相关代码再次变化、原证据不完整或用户明确要求复测时才允许重跑，并说明触发原因。
- 写代码保持最少行数，能简单实现就不要引入复杂抽象。
- 标准格式、协议、解析、压缩、加密、日期等通用能力优先使用成熟稳定的库，不要手写底层实现，除非用户明确要求或项目已有实现必须沿用。
- 不要为了“兼容更多场景”写大量分支，只实现当前明确需要的功能。
- 禁止用新增固定延时、轮询次数、重试次数、输出上限或其他拍脑袋常数掩盖状态与性能缺陷。限制只能来自供应商公开约束、管理员配置或项目已有且有测试依据的资源保护契约；没有依据时保持上游与现有配置语义，不自行降级性能。
- 项目尚未上线，不需要兼容旧数据；表结构或字段调整时直接按新设计修改，不写旧字段兼容、数据迁移兜底或删除旧表的清理逻辑，除非用户明确要求。
- 每次写完代码必须运行与改动相关的测试和类型检查；任务收尾按“Mandatory Testing”执行全量质量门禁与浏览器回归。
- 不要改无关文件，不要顺手重构。
- 如果工作区已有用户改动，不要回滚，不要覆盖；只在必要范围内追加修改。
- 含中文的源码、配置、脚本和文档统一保存为 UTF-8；PowerShell 读取时显式使用 `-Encoding UTF8`。发布验收必须严格解码文本文件，并检查 `U+FFFD`（替换字符）、`U+951F/U+65A4/U+62F7` 等常见乱码组合，不能把终端显示异常直接当成文件损坏。

## 反复提醒沉淀

- 如果开发过程中总是遇到某个问题，或者用户反复提醒同一个注意事项，需要把该注意事项补充到本文件。
- 补充时写成明确、可执行的规则，避免只写模糊描述。
- 新规则应放到最相关的章节；找不到合适章节时放到“项目注意事项”。

## 后端规范

- 后端使用 Next.js Route Handler + TypeScript，服务端代码位于 `web/src/app/api/` 与 `web/src/lib/server/`。
- Route Handler 只处理 HTTP 入参、鉴权、调用服务和响应映射，不堆业务规则或数据库细节。
- `web/src/lib/server/` 放业务服务、领域校验、任务编排和 provider 适配。
- 数据库访问统一沿用 `web/src/lib/server/database/` repository 与参数化查询；文件 Provider 只作为项目既有的服务端回退实现。
- PostgreSQL 在线功能必须按用户、实体、状态、时间窗、分页或聚合结果定向查询；禁止先读取完整数据库、整张业务表或大批 JSON 再在 Node.js/浏览器筛选。完整快照只允许用于管理员显式备份/恢复，并必须要求备份事务执行器；批量维护使用有界分页或批次查询。
- PostgreSQL `numeric/decimal` 字段允许小数；同一个参数占位符同时用于赋值、范围比较或表达式时必须显式写成 `$n::numeric`，不能依赖 PostgreSQL 从整数常量推断类型。相关 Repository 测试必须至少传入一个非整数值，并在可用时执行真实 PostgreSQL 回归。
- 共享同一个 PostgreSQL 测试库且包含建表、清理或锁操作的 Vitest 集成测试文件必须使用 `--no-file-parallelism` 禁用文件级并行，避免测试之间互相锁表造成随机死锁。
- 业务不变量在 service/helper 层校验，数据库唯一性、事务和并发约束在持久层保证。
- 业务接口保持 `{ code, data, msg }` 的响应结构。
- 管理员后台的支付、用户、系统设置、模型渠道与密钥、对象存储和业务备份操作统一使用当前管理员 Session、显式职责权限与脱敏审计，不得再次要求当前密码或 TOTP。可选 TOTP 只用于管理员登录挑战和本人 MFA 启用/关闭；本人修改密码、注销申请等用户自证操作继续按各自契约验证当前密码。
- 管理员设置页面和 `/api/admin/settings` 的 GET/PATCH 合并读取必须绕过进程内短缓存并刷新当前模块缓存；短缓存只用于不要求读后写一致性的普通运行时读取。保存或删除回归必须覆盖操作后立即可见、服务端已持久化、页面刷新和 API 再读取四个状态，禁止等待缓存自然过期。
- 新增数据表时同步更新 `docs/backend-database.md`。
- 上游生成任务接口版本与模型目录是独立契约：只有供应商公开并验证过的目录路径才能加入协议注册表或由管理员显式配置；没有目录时只能使用官方文档明确列出的模型预置或手动模型 ID，禁止根据 V2 任务路径猜测 `/v1/models` 或未确认的 V2 目录。
- 自定义协议必须从用户提供的任意上游接口文档地址提取真实目录、创建、查询、取消、请求体和结果字段，禁止写死案例域名、案例路径或依赖当前页面作为回退。涉及协议解析、系统代理或生成入口的改动，必须使用本地自建上游 fixture 逐项验证统一创作、图片、视频、音频、Canvas 和短剧实际支持的全部协议请求与返回；不能只做解析器单测或依赖外部供应商环境。
- 生成任务的 `needs_review` / `submission_outcome_unknown` 表示上游提交结果未知，不等同于参考素材无效。服务端必须按执行阶段与上游状态统一解析公开原因，不能把基础设施脱敏文案误当成任务状态；Canvas 等已持久化节点只显示一次可操作说明，不得再叠加相同全局 Toast。
- 已保存上游任务 ID 的 `needs_review` 不得只依赖管理员处理。用户“检查状态”必须对同一个上游任务执行一次查询，并在结果已返回时继续落盘；上游仍在处理或本地保存暂时失败时回到可再次检查状态，禁止重建任务、重复计费或重新开启无限自动轮询。
- 系统渠道代理返回的 `x-vozeb-pro-upstream-url` 只用于解析上游媒体相对地址，不得覆盖任务的 `pollBaseUrl`；异步创建与恢复轮询必须继续使用原站内 `/api/ai/system/{channelId}` 基址，确保鉴权、任务归属和渠道协议映射仍经过系统代理。

## 前端规范

- 前端使用 Next.js App Router、React、TypeScript、Ant Design、Tailwind、Zustand。
- 编写 Ant Design 相关代码时，参考 https://ant.design/llms-full.txt 理解组件 API、示例和设计规范，并优先结合项目当前 antd 版本与既有写法。
- Ant Design 组件根节点可能覆盖 Tailwind 的 `display`、宽度和响应式布局；`Checkbox.Group`、`Select`、`Segmented` 等组件需要网格或断点尺寸时，使用普通 `div` 外壳控制布局，组件本身只负责控件行为。浏览器回归必须读取最终 `display`、行列坐标和宽度，禁止只统计列数或只检查源码 className。
- API 请求统一放在 `web/src/services/api/`。
- 图片、视频和 Canvas Agent 必须先由后台默认文本模型完成规划，主动替用户选择能力匹配的逻辑模型与参数；规划结果只用于内部执行，不要求用户自行猜选模型。
- 普通 Skill 只能由用户本轮显式选择后使用；未选择 Skill 时服务端不得向规划器暴露普通 Skill，也不得接受规划结果自行补选 Skill，但仍必须执行默认提示词优化、视觉方向、模型与参数规划。
- 所有 Agent 可以复用同一份已解析公共模型目录，但模型选择 UI 必须显式限制为当前 Surface 实际支持的能力：生图工作台只显示图片模型且不显示单项分类栏，视频工作台只显示视频与音频模型，统一 Agent、Canvas 和短剧按各自真实能力显示；不得因为共享目录包含其他模型就在不支持的工作台展示对应分类。
- `/create` 统一 Agent 输入框改版时必须同时保留附件、Skill、逻辑模型选择、智能规划，以及 Agent、图片、视频、音频四种创作类型；图片偏好至少包含比例和画质，视频包含比例、清晰度和时长，音频包含音色和格式。Agent 与模型入口沿用项目既有 `Lightbulb`、`Orbit` 图标语义；所有选择必须接入真实 Agent Run、持久化和失败重试，不得只做无后端行为的视觉控件。
- `/create` 输入框底部的创作类型、模型与生成偏好必须保持单行紧凑摘要；模型列表、比例、画质/清晰度、时长、音色和格式只在点击对应入口后展开。工具入口的激活配色只允许在对应弹层打开期间出现，选择完成并关闭后必须恢复中性配色；已选值通过文案、图标或数量摘要表达，禁止用常驻高亮底色冒充打开状态。窄屏允许左侧工具条横向滚动，但发送按钮必须独立固定可见，禁止重新把模型卡片或参数网格常驻铺在输入框内。
- `/create` 参考 GPT、豆包、即梦等成熟产品时只借鉴“需求与参考素材 → 最终公开模型/参数 → 生成结果 → 后续操作”的信息层级，必须按 VOZEB PRO 实际能力做混合布局，禁止逐像素复制第三方页面、品牌、文案、图标、字体、素材或专有 token。图片/视频/音频 Run 按稳定 `runId` 组合为对话式创作记录：用户需求在右侧上一行并显示用户头像，助手结果在左侧下一行并显示站点 Logo，禁止再把结果与需求横向拉成同排双栏。普通文本问答继续使用聊天布局。公开摘要只显示实际模型、尺寸/比例、画质/清晰度、时长、音色、格式、数量和状态，不得暴露内部规划或执行提示词。
- `/create` 图片与视频 Result Card 禁止为了视觉统一设置固定大宽高或最小高度。主媒体先使用后端有效 `width/height`，缺失时读取图片 `naturalWidth/naturalHeight` 或视频 `videoWidth/videoHeight`，再回退真实比例 metadata 与安全默认值，并在有界最大尺寸内等比计算 `renderedWidth × renderedHeight`；媒体、预览容器和可见 Card 必须同宽同高，不得用 `cover` 拉伸或让媒体只占 Card 一角。结果组使用 shrink-to-fit，只有操作栏真实放不下时才允许比媒体略宽。图片与视频统一只按实际 ready 且有真实 URL 的结果数组判断多结果：`results.length <= 1` 时“更多”及切换器 DOM 必须完全不存在，`results.length > 1` 时才渲染；桌面缩略图紧跟主媒体右侧，移动端位于主媒体下方，操作栏不得参与两者间距计算。生成完成时间与耗时作为标题同行的弱化元信息，不得独占媒体上方一整行。
- `/create` 右侧资产面板的当前对话、我的提示词和提示词库统一使用每行 4 个缩略图，并在真实滚动内容区保留左右内边距，首尾缩略图不得贴住面板边界。图片与视频点击后按媒体真实比例展开，媒体边框只包裹媒体本身，不使用固定黑色大画布；内容图片和预览继续请求限宽 WebP 变体。当前对话的“引用/已引用”和提示词的“插入”必须位于对应缩略图下方，不得覆盖画面；引用选中态使用项目主题色边框与文字状态，不显示 Checkbox 或右上角勾选。图片/视频引用进入输入框后只显示紧凑媒体缩略图，标题只用于 `alt`、悬停提示或无障碍名称；通过 `@` 选择资产时必须移除本次 `@查询` 文本并保留真实 `assetId`，不得再把 `@素材标题` 写入公开草稿。提示词不展示标题和正文，分类使用主题感知的紧凑菜单并按服务端 `category` 参数重新请求。
- `/create` 媒体创作记录只有失败轮次显示“直接重试”，点击后必须复用原 `conversationId`、用户/助手消息、Run 或失败提交的稳定请求身份，在原轮更新状态并直接重新执行；不得恢复输入框、要求用户重新输入、追加一轮同名消息或新建对话。失败 Run 有多个失败子任务时必须用一次请求在同一 Run 内原子重试全部失败项，只做一次 Run 更新和一次调度；禁止前端逐个请求让 Worker 与后续请求竞争覆盖，并发额度不能把该 Run 自身重复计数。规划失败重试原 Run，创建请求尚未落成 Run 时复用原幂等请求。成功轮次禁止显示“重新编辑”或“再次生成”，结果操作只复制服务端显式白名单的 `optimizedPrompt`，并保留下载和当前结果引用，不提供“新窗口打开”。`optimizedPrompt` 必须是规划器产出的公开提示词，不得包含平台 foundation、direction、Skill 执行规则、依赖上下文或其他内部执行内容；用户消息旁始终提供原文复制，图片、视频和普通对话使用同一规则。
- `/create` 视频结果的 `<video src>` 由 React 负责；effect cleanup 只能暂停播放，禁止 `removeAttribute("src")`、清空 `src` 或调用 `load()`。React Strict Mode 回归必须确认开发模式二次 effect 后媒体 URL、`readyState`、固有宽高和时长仍有效。播放器控件按媒体容器宽度响应，不按 viewport 判断显隐；桌面、390px、430px 与窄竖屏结果都要读取播放器和最右控件边界，确保全屏按钮不溢出。
- `/create` 输入框的“优化提示词”必须调用后台默认文本模型并将结果回填为可编辑草稿，不自动发送；请求必须防重复计费，并沿用后台模型与渠道的真实能力配置，禁止额外写死输出 token 上限。用户继续编辑或发送后，迟到的优化响应不得覆盖新内容；只有用户最终发送的公开草稿才能进入可见用户消息及其原文复制操作。
- `/create` 输入框中的已上传图片或视频必须直接占据左侧“添加参考素材”槽位并展示真实缩略图，文件名只作为 `alt`、悬停提示或无障碍说明，禁止在输入框上方另起一行渲染“媒体图标 + 文件名”标签；视频首帧与尾帧继续在同一输入行的专用角色槽位并列展示，不能在普通附件区重复出现。
- `/create` 在用户发送前选择的附件必须只保存在页面私有内存草稿中，不得提前创建服务端会话或上传媒体；只有发送时才创建会话并把草稿转成稳定资产，退出登录、新建对话、打开历史对话、移除附件或转成稳定资产时必须释放对应 `object URL`。输入区不得另起一行显示“上传中”，真实发送阶段只允许上传入口自身显示忙碌状态。普通媒体缩略图删除按钮的触控命中区不得小于 28px，但可见圆形角标保持 22–24px、默认使用高对比中性色与白色图标，悬停或聚焦时再切换为柔和删除语义色；角标应部分外置，不能大面积遮挡媒体主体、碰到继续添加入口或依赖悬停才可辨认。空白新对话的创作类型、模型、参数与 Skill 弹层向下展开，进入对话后统一向上展开。
- `/create` 参数弹层必须按触发器相对 `visualViewport` 的实际剩余空间限制可滚动高度，不能只用整屏高度导致手机端选项落到视口外。Playwright 必须使用正常语义点击验证选项真实可见可操作，禁止用 `force`、固定重试或延时掩盖定位错误。
- `/create` 视频模式必须提供普通参考、首帧和首尾帧三种真实引用方式；首帧与尾帧只能由用户显式选择上传或已有图片，首尾帧模式缺任一角色时必须在创建任务前阻止提交，同一图片不得同时承担两个角色。角色图片在专用槽位中只显示一次，不能再作为普通附件重复展示或重复提交。
- Canvas、生图工作台、视频工作台、统一 Agent 和短剧的生成尺寸必须遵循同一优先级：用户本轮文字明确尺寸 > 当前精确自定义宽高 > 本轮参考图比例 > Agent 规划尺寸 > 后台默认尺寸。文字未要求尺寸时不得用历史记忆或规划结果覆盖当前自定义尺寸；没有精确自定义尺寸但有参考图时必须继承参考图比例。Canvas 的当前精确尺寸可能保存在配置节点 `metadata.size` 而根级 `imageSize` 仍是默认比例，服务端必须读取真实紧凑快照，不能只依赖根字段。图片任务使用精确 `宽x高` 时，服务器必须在落盘前检查上游真实文件并规范化到目标宽高，不能只验证请求参数。
- 所有生成入口中的“智能/自动”比例、画质或清晰度都表示用户没有锁定固定值：模型切换不得把 `auto` 收敛成能力列表首项，服务端能力档案只校验用户明确选择的固定参数，任务归一化继续按本轮文字、当前精确尺寸、参考素材和 Agent 规划解析；仍未确定时向支持的上游省略该字段或发送供应商公开的自适应值，禁止统一改写为 `1:1`、`16:9`、`720P` 等拍脑袋默认。
- 图片、视频、Canvas 和短剧的自定义尺寸不得再叠加平台统一最大边长、总像素或宽高比上限；服务端只校验尺寸能解析且为正整数，真实上游拒绝时直接透出上游错误。媒体解码与重采样可以保留独立的资源保护，但不要把它包装成模型能力限制。
- 图片、视频、Canvas、短剧和统一 Agent 的平台规划提示词、改写提示词、模型选择理由、创作简报、视觉方向、物料计划与复盘详情只能用于内部执行，不得显示或持久化到生成型对话；Agent 应先自然确认用户需求再调用生成模型，结果返回后只给简洁完成说明，普通非生成问答可保留自然语言回复。
- 图片/视频工作台生成日志必须显式区分用户本轮原文与 Agent 改写后的上游执行提示词：用户原文只用于标题、历史搜索、对话恢复和用户可见消息，执行提示词只用于任务创建与显式重试；会话或用户原文尚未恢复时宁可不显示用户消息，也不得用执行提示词回退。相关改动必须同时覆盖新建、持久化 round-trip、历史加载中、历史加载完成和重试。
- 图片/视频工作台持久消息必须由服务端显式标记为公开内容，列表、搜索、分页详情和前端恢复都只接受该公开标记；没有标记的旧消息或未知来源消息按内部内容处理并禁止展示，不能仅凭 `role="user"` 判断可见性。
- 图片/视频工作台每次用户主动提交都必须使用新的请求和生成记录身份，即使提示词、参数和参考素材完全相同也不得按内容合并；只有用户对同一失败结果显式点击重试时才沿用原会话、生成记录与结果槽身份，并创建新的上游尝试。消息、会话和生成记录关联只能使用稳定 ID，禁止按提示词文本匹配或去重。
- 图片/视频工作台的服务端渲染输入框在 React 水合前允许输入时，客户端必须先接管 DOM 中的非空草稿：首次 `空用户 -> 当前登录用户` 水合不得清空；只有切换已登录用户、退出登录、切换工作台或用户明确新建对话时才清空。浏览器回归必须覆盖 `domcontentloaded` 后立即填写、再等待 Session 水合的顺序。
- 图片/视频工作台的结果框必须按当前 `creativeConversationId` 聚合展示该对话全部独立生成记录的 pending/failed/success 结果，不能只显示或覆盖成最新一轮；只有用户点击“新建对话”才进入空结果框，切换历史对话或刷新后必须恢复该对话全部轮次。聚合只发生在展示层，删除、重试、缺失清理和选择仍使用 `recordId + resultId` 定位真实记录，禁止把旧结果合并写入最新生成记录。
- 图片/视频工作台历史抽屉每个 `creativeConversationId` 只显示一条会话记录，但缩略图、成功/失败/生成中数量、结果总数和耗时必须汇总该会话全部生成轮次，不能只显示最新日志的统计。自动标题只在新对话第一轮初始化，后续输出不得自动改名；只有用户行内重命名可以覆盖标题。
- `/create` 历史抽屉必须把新建操作区与可滚动记录区分开；记录支持桌面悬停/移动端可见的管理入口、行内改名、单选、全选和批量删除确认。普通文本问答与媒体 Run 统一使用右侧用户请求气泡、时间和服务端会话用户头像，左侧助手使用同一套站点 Logo 容器；媒体 Run 使用同轮创作记录，每轮只显示一次右侧用户头像和一次左侧助手 Logo，不重复渲染消息或头像。
- 用户离开或刷新 `/create` 后，页面必须从服务端查找最近的 `planning/running/paused` Agent Run，自动打开原会话并重连 SSE；工作台运行任务入口必须携带稳定 `conversationId` 返回原对话，不能把 Agent 子任务错误跳到图片或视频工作台。
- 全局或跨页面状态优先放在 `web/src/stores/`。
- 已经放在全局 store 或全局 hook 中的状态/动作，组件需要时直接使用对应 store/hook，不要为了“纯组件”层层透传 props；避免一个组件传递过多参数。
- 全局组件、全局常量、全局配置等全局性质的内容不要作为 props 或参数层层传递；哪里需要就在哪里直接从对应全局入口获取。
- 多个页面重复出现的 UI 副作用动作，例如复制文本并提示、下载并提示、统一确认弹窗，优先抽成 `web/src/hooks/` 下的全局 hook；不要放进 store，除非它确实是需要共享/订阅的状态。
- 画布相关状态和组件放在 `web/src/app/(user)/canvas/` 内部。
- Canvas 图片分层（电商、人物和普通主体）必须只做一次文本/视觉元素识别；识别返回的每个元素使用同一张完整主图作为唯一上游参考，并分别创建独立的透明图片任务和普通图片节点，同时启动所有元素任务及背景补全任务。禁止把本地 bbox/矩形裁片作为上游参考、把多个元素合成集合节点、用浏览器分割 Worker、占位图或本地裁切结果冒充分层结果。每个元素节点必须可独立移动、保存和下载，源图分别连向每个元素和背景节点；任务失败时必须结束对应 loading 并显示明确失败/待检查状态，不得伪装成本地成功。元素结果必须保持上游返回的完整尺寸和原坐标，落盘不得再次缩放、裁切或重采样。独立“消除背景”工具仍可使用既有本地语义分割。任务数量、输出数量和处理顺序由视觉识别结果与上游协议决定，平台不得自行附加固定常数、轮询次数或输出上限。
- 页面里只有一个主业务组件时直接写在 `page.tsx`，不要单独拆 `Manager` 组件再传一堆 props。
- 不要新增只做简单转发的组件，例如只 `return <X>{children}</X>` 或只换个名字透传 props；直接在使用处使用真实组件或把逻辑写进当前文件。
- 页面私有 hook 放在对应页面目录下，例如 `admin/assets/use-admin-assets.ts`；只有多个页面真实复用的 hook 才放到外层 `hooks/`。
- 管理后台页面私有组件放到各自页面目录的 `components/` 下，例如 `admin/assets/components/`、`admin/prompts/components/`；不要为了单页面使用放到 `admin/components/` 共享目录。
- 管理后台分区只能在用户指针、触控或键盘意图时预加载目标分区，禁止空闲时串行预加载全部分区；分区聚合文件和通用 helper 只能导入自身真实使用的模块，不得因为复制拆分残留而把账单、运维、媒体、表格或其他后台页面重新带入首屏包。
- 管理后台主题、背景、卡片阴影、表格配色等统一在 `web/src/lib/app-theme.ts`、`AppProviders` 或必要的全局 CSS 作用域中配置；页面私有组件不要自己写 `dark ? ...` 主题分支。
- 管理后台侧边栏按商业 SaaS 职责分组和排序：经营分析、商品运营、财务管理、上游配置、系统管理、内容运营；支付渠道、CDK 兑换和财务流水必须归入财务管理，模型渠道和 Agent Skills 必须归入上游配置，本地媒体、外部存储和数据备份必须作为系统管理下的独立页面；菜单文案使用清晰业务名词，避免模糊短语。
- 后台列表管理页默认采用“列表/卡片 + 创建按钮 + Modal/Drawer 表单”的 SaaS 工作流；新增或编辑商品、公告、用户、配置项等操作不要把大表单常驻铺在页面主体里，除非该页面本身就是专门的配置向导。
- 后台设置的保存与删除必须让本地 `settings` 快照和提交 patch 保持一致；并发响应保护不得把本次删除误判为请求期间的新编辑。删除回归必须同时断言操作后立即消失、服务端已持久化且刷新后不会恢复。
- 后台配置类表单要像商业 SaaS 设置页：短字段必须用自适应紧凑网格铺满当前行，长文本/密钥/证书字段单独通栏；不要让一个短输入单独占一行形成大面积空白。
- 后台支付配置等密集设置页的普通单行输入框必须保持紧凑高度，字段卡片不能被同一行的长文本字段拉伸；但字段标题、状态标签和输入框之间必须保留清晰垂直间距，不要贴在一起；只有私钥、证书、模板、额外请求头等长内容允许使用较高文本域。
- 后台手机端和平板窄屏顶部不重复显示当前分区标题和副标题；标题已在主内容卡片中展示时，顶部只保留菜单按钮、常用操作和初始化入口，常用操作应与菜单按钮同一行，初始化入口在下一行。
- 后台移动端抽屉关闭按钮必须和当前浅色/深色主题融合，不要在深色抽屉里使用突兀的大白色圆形按钮。
- 后台 Drawer 不得直接使用会在手机端保持固定大宽度的 `size="large"`；必须使用不超过 `100vw` 的响应式宽度，并在 390px、430px 检查抽屉根节点、内容区和底部操作区无裁切或横向溢出。
- 用户侧手机端不得直接沿用桌面的固定大高度、大封面比例、大卡片内边距或 Ant Design 默认大插画空态；列表卡、加载态和空数据提示必须在 `sm` 以下使用紧凑尺寸，保证 390px/430px 首屏能看到下一层核心内容，同时保持可读间距。每次相关改动都要检查页面与内部滚动容器无横向溢出。
- 短剧项目详情必须保持独立全尺寸生产工作区：顶部项目工具栏下的唯一集级阶段导航只包含剧本、内容审核、分镜和镜头生成；角色、场景、道具、线索及来源素材属于整个项目，必须通过独立“项目资产”入口管理，不得作为当前 Episode 的生产阶段或只绑定当前集。阶段状态只能由真实剧本、审核、分镜和生成数据派生；剧本阶段在 1366px 及以上常驻集数管理侧栏，中央使用场景结构与纯文本剧本编辑器，右侧常驻本集设置/项目 Agent，低于该宽度再折叠为响应式 Drawer。所有侧栏关闭后不得占用生产区宽度；镜头生成必须依据真实审核、提示词、资产引用、分镜、视频、配音和合成状态展示阻塞原因、精确失败项与单镜头重试。相关改动必须回归 1672px、1440px、390px、430px、浅深主题、滚动边界和无横向溢出。
- 短剧项目资产页使用单层紧凑工具栏和高密度卡片库；空态不得重复工具栏已有的“新建”主操作。新建角色、场景、道具和线索使用不超过 640px 的响应式紧凑 Modal，只填写初始设定，不展示无效的参考图大占位；已有资产编辑继续使用右侧 Drawer 管理基准图与候选图。状态筛选、排序和来源素材只按现有项目数据派生或按需展开，不得为视觉展示写入假资产、Episode 或 Scene 数据。
- 短剧项目资产筛选可以按当前 Episode 的真实镜头资产引用派生“当前集涉及”，但不能改变项目级资产归属；项目基准图打包下载只读取真实候选/基准媒体，附带资产清单并准确报告缺失或失败项，不创建媒体副本或假数据。
- 短剧项目 Agent 入口固定为顶部全局操作区中“版本记录”之后的单个机器人图标按钮，不得再放到生产区右侧边缘或重复显示文字入口；桌面点击后使用与 Canvas 一致的同层右侧协作面板并让生产区自然收缩，不使用遮罩或多层 Drawer，窄屏才使用不超过视口的响应式 Drawer。
- `/create` 灵感作品、作品广场、个人作品和创作者主页的瀑布流必须先按 DOM 顺序从左到右铺满当前首行，后续作品再进入当前最短列；禁止使用会按纵向阅读顺序填充的 CSS `columns-*`。响应式列数保持 2/3/4/5/6，浏览器回归必须用不同宽高比夹具读取真实卡片坐标，断言首批同顶边且 `left` 递增、下一项进入最短列，并检查桌面、390px、430px 无横向溢出。
- 生成结果的 pending/failed 占位必须在真实父布局中具有可解析的宽高和可见状态文案；禁止依赖自动宽度 flex 子项里的百分比子元素反推尺寸。回归必须读取浏览器实际 `getBoundingClientRect()`，不能只用源码字符串断言 Tailwind class 存在。
- 图片与视频工作台的生成中占位必须复用同一组件，并呈现无可辨识白色圆心、颗粒或方块边界的连续烟雾混色；动画只允许少量预渲染纹理层使用 `transform` 和 `opacity`，禁止大量独立动画节点、运行时模糊滤镜或循环重绘。相关改动必须在两个工作台的真实结果布局中截图，并验证生成期间滚动无明显卡顿。
- 图片上游一次返回多份结果时，服务端必须完整落盘、登记并保留兼容主结果；图片工作台、Canvas 和短剧候选资产等支持多结果的界面必须展示全部成功结果，单份失败不得隐藏同批成功结果。只允许单主图的业务实体可以选择首张作为主图，但其余结果仍必须保留在生成记录和资产关联中。
- 所有按钮、图标按钮、侧边栏菜单项和自定义可点击项必须显式提供浅色/深色/hover/disabled 可读状态；主按钮深色底时文字必须是白色，深色主题反转为浅底时文字必须是深色，不允许出现黑字黑底、灰字深底或图标与背景对比不足。
- 待审核、审核中、排队中、生成中等过程状态不得使用纯黑或深灰实心标签；统一使用语义浅底、细描边和可读文字，深色主题使用低透明语义底色，避免和主操作按钮争夺视觉层级。
- 管理后台凡是展示、筛选或选择用户的页面，必须统一使用公开账号 ID：显示格式为 `ID：0001`，不足四位补零，达到五位、六位后自然扩展；输入 `0001` 必须能搜索到对应用户。界面禁止显示内部 UUID，也不得用 UUID 作为用户信息缺失时的回退；优先复用 `AdminAccountId`、`AdminUserIdentity` 和 `AdminUserSearchSelect`，桌面密集表格可拆分“用户 / 账号 ID / 邮箱”，手机端使用紧凑合并身份信息。
- 后台深色信息卡片里的链接按钮必须显式设置背景、边框、文字和 hover 对比，并使用足够优先级覆盖全局链接/按钮样式；浅色主题黑底卡片用白字白边，深色主题反转白底卡片用深色文字和深色边框。
- 全局 `html, body` 固定为 `height: 100%; overflow: hidden;`，所有不在 `(user)` 工作区布局内的独立页面必须用 `.app-scroll-page` 作为整页滚动容器；`(user)` 工作区子页面内容可能超过视口时，页面根节点必须提供 `h-full min-h-0 overflow-y-auto` 或同等内部滚动容器。
- 全站页面、侧栏、抽屉、弹窗、列表和编辑区域不得显示浏览器原生滚动轨道或滑块，但必须保留鼠标滚轮、触控板、触屏和键盘滚动能力；长下拉列表隐藏滚动条后使用轻量上下方向提示，不得通过 `overflow: hidden` 截断内容来伪装。
- 组件优先使用函数组件和现有 hooks，不新增大型状态管理方案。
- UI 图标优先使用 `lucide-react` 或项目已经使用的 Ant Design 图标。
- 所有默认站点品牌 Logo、ICO、favicon、manifest/metadata 图标、登录/用户端/后台品牌标识和助手头像必须使用项目的“无限进化”Logo，并统一从后台站点 Logo/图标配置或内置 `/logo.svg`、`/icon.svg` 派生；禁止使用三角形作为品牌 Logo、默认占位或回退图标。播放、告警等功能性三角图标不属于品牌标识，仍按语义使用。
- 管理后台的作品、素材、提示词封面、本地媒体和外部存储等内容图片必须使用限宽 WebP 变体展示，禁止把 PNG/JPEG 原件或对象存储源文件签名地址直接放入图片 `src`；只有明确的下载操作返回源文件格式。站点 Logo、ICO、SVG 品牌资源不属于内容图片，不受此转换规则影响。
- 对象存储 WebP 预览只能以原始媒体对象为输入，预览对象本身不得再次生成预览；内部 `.vozeb-preview/` 对象不进入管理员文件列表。历史异常清理只允许删除包含多层 `.vozeb-preview/` 的递归预览，必须保留原始对象和合法首层预览，并记录管理员审计结果。
- 作品发布、公开分享和作品广场只允许图片与视频，不提供音频、收藏或评论；用户或管理员下架后可二次确认永久删除发布主记录、版本快照、治理与互动记录，但必须保留源素材、Canvas/短剧项目和媒体文件，禁止恢复“撤销后长期保留发布快照”的旧行为。
- 后续通用 UI 可以参考 https://uiverse.io/elements 或用户提供参考图/HTML 的交互和视觉方向，但必须改造成项目现有 Next.js / React / Tailwind / Ant Design 写法；不要直接复制第三方源码、图片、字体、图标、素材、专有 token 命名或受限资源，确需引入第三方资源时必须先确认授权许可证。
- 页面文案保持中文。
- 不要在组件里堆太多无关逻辑；复杂逻辑优先抽成同目录工具函数或小组件。
- 样式优先由组件自己管理；组件私有样式优先使用 Tailwind className 或少量内联 style，不要为单个组件新增大量全局 CSS。
- 全局 CSS 只放基础变量、全局重置、跨页面通用样式和少量第三方组件必要覆盖；不要在 `globals.css` 堆页面私有样式。
- 代码尽量短小直接，少拆不必要组件，少做多层 props 传递，避免为了抽象堆出更多代码。
- 创作会话、工作台记录、Canvas、素材库、短剧和媒体等业务数据必须保存到服务器，并按登录用户恢复；浏览器只保留页面瞬时状态和主题等极小偏好，不得使用 localforage、IndexedDB 或 localStorage 持久化业务列表、生成记录、图片、base64 或大 JSON。
- 服务器本地媒体写入必须登记用户归属、来源、原文件名及可用的会话/Run/任务/项目关联；用户读取必须校验归属，外部上游只能使用短期签名 URL。管理员批量删除、迁移和临时文件过期清理必须先检查创作会话、素材库、Canvas、短剧、生成记录、头像与作品引用，不得使用 `force` 绕过引用保护。
- 用户界面明确写“删除”时不得在服务端偷换成归档。删除创作会话、Canvas、生成记录、素材或媒体时，必须按 `user_id + storageKey` 定向查询命中实体，在同一 PostgreSQL 事务或文件 Provider 多文件锁与回滚边界内移除消息、素材库、其他 Canvas、短剧、生成任务/记录、头像和发布作品中的对应引用；禁止读取整库或整张业务表到 Node.js 再筛选。事务提交后删除媒体登记及物理文件；登记为对象存储时必须同时删除 OSS 原对象和其 WebP 预览变体。读取端不得继续返回已删除 URL、空媒体卡或 404 占位；重复删除必须幂等，并发产生的新引用仍由最终引用复查保护。
- Canvas、Agent、图片/视频工作台、作品发布和素材库引用已经登记的站内永久媒体时，必须直接复用原 `storageKey` 与 `/api/reference-assets/` 或 `/api/generation-log-assets/` 地址并保留真实作用域；禁止先下载再上传生成副本。只有 `File`、`Blob`、`data:`、外部 URL 或需从临时存储转为永久存储的媒体才允许新写入。
- WebP 变体只用于浏览器展示，禁止作为图片生成、视频生成、Canvas Agent 或短剧提交给上游的参考源。上游参考必须复用去除 `format/width/download` 参数后的稳定源文件；源文件本身为 PNG/JPEG 时直接提交，源文件本身为 WebP/GIF/AVIF 等上游不支持格式时只能在服务端按真实字节无缩放转为 PNG 临时引用，不得覆盖源文件或从预览 `src` 回读后再次上传。
- 创作会话与消息列表必须使用服务端分页；打开长会话时默认加载最新消息，并支持向前加载历史，禁止固定读取最早 N 条导致新消息在刷新后消失。

## 画布 UI 规范

- 做 canvas 前端 UI 时必须遵循当前画布主题。
- Canvas Agent 输入区的 40px 参考素材缩略图删除入口必须保留至少 28px 触控命中区，但可见圆形角标固定为 16px 并完全收在缩略图边界内；不得外置、不得造成面板或页面溢出，浅色/深色主题默认使用中性色，悬停或聚焦时才切换为柔和删除语义色。
- Canvas Agent 输入框中的 `@图片/@视频` 高亮预览必须使用固定裁切外壳，只允许内部文字层随 textarea 的 `scrollTop` 位移；禁止移动裁切外壳或让高亮文字溢出到消息区、把历史提示词顶到输入框上方。
- Canvas Agent 输入框底栏固定按 `Skill → 智能规划 → 模型 → 生成参数 → 发送` 排列，前四个工具入口连续靠左成组，生成参数使用调节/配置语义图标；发送按钮独立固定在最右侧，禁止用 `margin-left: auto` 把生成参数推向发送按钮。Canvas Agent 专用生成参数摘要按钮宽度不得超过 116px，参数浮层目标内容宽度为 280px；调整这两个尺寸时不得连带压缩输入框高度、内边距或发送按钮。浏览器回归必须读取触发按钮与 Portal 内容的最终矩形，不能只检查 className。
- Canvas 视频节点与视频生成配置节点必须提供普通参考、首帧和首尾帧三种显式引用模式；首尾角色只能由用户从已连接或已保存的图片中选择，不自动猜测、交换或降级，同一图片不能同时承担首帧和尾帧。新生成输出节点必须保存引用模式、角色素材、提示词、模型与参数快照；用户对失败结果显式重试时沿用该输出快照并创建新的上游请求身份，不得受源配置后来改动或断开连线影响。只有旧项目没有快照时才允许回退当前连线。
- Canvas 在已有图片节点上直接生成视频时，当前图片节点必须作为本轮唯一主图引用写入上游请求和输出节点快照；禁止因为通用上下文排除节点自身而退化为文生视频，也不得改取更早的上游图片、其他画布图片或历史媒体。配置/文本节点生成视频时仍只读取其本轮明确连接的参考素材。
- 优先使用 `canvasThemes`、`useThemeStore` 或 Ant Design `ConfigProvider` token。
- 不要硬编码黑白、stone、slate 等颜色导致浅色/深色主题不一致。
- 新增画布按钮、弹窗、浮层时，尽量复用已有工具栏、节点面板、Modal 的视觉风格。
- 画布顶部工具栏和状态信息优先采用极简扁平风格：无边框、无阴影、无胶囊背景，融入整体背景，弱化按钮感，仅保留轻微 hover 反馈，保持简洁现代、低视觉重量。
- 图片节点尺寸逻辑要尊重原始比例，除非功能明确要求自由变形。
- Canvas 图片编辑弹窗采用“真实媒体预览 + 参数栏”双栏时，两栏只齐顶、不得由 Grid/Flex 默认 stretch 互相拉高；预览边框只包裹图片等比渲染尺寸，不得留下大块空画布。标题与状态使用相同盒高并保持同一水平线，模式操作放在参数栏顶部；选中项只通过控件选中态表达，不重复显示同值摘要。Ant Design `Slider` 端点标记必须由普通外壳预留左右文字宽度，不能只给 Slider 根节点加 margin。浏览器回归必须读取标题/状态、预览/图片、两栏、端点文字和关闭按钮的真实矩形，并覆盖桌面、390px、430px 与浅深主题。
- 生成、导入和历史恢复的图片节点外框必须匹配图片真实宽高比；上游结果缺少宽高时，在图片加载后读取自然尺寸、保持节点中心点不变地回写并持久化，自由变形节点不得被自动改回原比例。
- Canvas 节点内部的大面积预览不得拦截节点根级拖动；只有按钮、输入框、媒体控件等明确交互区域阻止冒泡。节点内打开的 Modal/Popover 等 Portal 必须隔离返回组件树的点击、指针、滚轮和右键事件；沉浸查看器等可拖动内容不得用遮罩点击关闭，必须回归“未打开可移动节点、打开后不移动节点、边缘拖动不误关闭、关闭按钮有效”。
- Canvas 一键整理不能只消除节点重叠；必须按弱连通分支划分独立布局区间，并在分支内按拓扑与父子重心排序，避免不同生成链路的连线互相穿插、兄弟连线交叉或长线穿过无关节点。浏览器回归至少使用两组旧坐标互相穿插的分支，读取整理后节点区间和真实 SVG 路径采样点，不能只断言节点矩形不重叠。
- 批量生成、多图展示、助手面板等画布交互要尽量简洁，不要占用过多画布空间。

## 文档规范

- README 保持简洁，只放项目介绍、核心功能、快速开始和文档入口。
- `docs/index.md` 放给 AI 使用的文档索引，不要再放到 `docs/content/docs/` 内容目录里。
- 详细功能介绍写到 `docs/content/docs/overview/features.mdx`。
- 后续待办写到 `docs/content/docs/progress/todo.mdx`。
- 已实现但还需要用户测试确认的事项写到 `docs/content/docs/progress/pending-test.mdx`。
- `docs/content/docs/progress/pending-test.mdx` 用来记录这个版本实际做了哪些可测试变更；`CHANGELOG.md` 的 `Unreleased` 只保留对这些变更的版本级归纳，避免逐条照搬实现细节。
- 每次 todo 事项完成后，先从 `docs/content/docs/progress/todo.mdx` 移到 `docs/content/docs/progress/pending-test.mdx`，不要直接写进正式功能说明；用户确认测试通过后再更新 `docs/content/docs/overview/features.mdx`。
- 每次任务完成前，都要根据实际变更检查并更新 `docs/content/docs/progress/todo.mdx` 和 `docs/content/docs/progress/pending-test.mdx`；如果功能或待办没有变化，也要确认无需修改。
- 涉及商业能力、用户权利、支付、合规、运营、监控、灾备或版本收尾时，必须重新核对 `docs/content/docs/overview/production-readiness.mdx`，并同步 todo/pending-test/CHANGELOG 的真实状态；依赖支付商、法务、监控平台或运营制度的事项不得因为代码存在基础能力就标记为已完成。
- 接口响应规则写到 `docs/content/docs/backend/api-response.mdx`。
- 数据库结构写到 `docs/content/docs/backend/backend-database.mdx`。
- 文档不要写过期日期；除非用户明确要求记录具体时间。

## 发版本流程

- 发版本时，先把 `CHANGELOG.md` 的 `Unreleased` 变更整理成新的版本记录，并保留空的 `Unreleased` 标题。
- 按当前版本号提升一个版本，更新根目录 `VERSION`。
- 将当前未提交的代码全部提交到 Git。
- 提交完成后，给当前提交打最新版本号对应的 tag，例如 `v0.0.6`。
- 发版本流程中不要执行编译、测试或构建，除非用户明确要求。

## 项目注意事项

- 管理后台站点配置中的非空社交或联系地址必须在 HTTP 边界校验：可识别的简写应规范化为标准 URL，无法识别时返回明确 4xx，禁止 normalizer 静默改为空字符串后仍返回保存成功。相关改动必须覆盖后台填写保存、刷新回读、公开 Session 与首页入口的完整 round-trip，并断言无效输入没有写入持久层。
- Playwright 的应用、协议和支付夹具必须使用每次运行前确认空闲的独立端口，并禁止 `reuseExistingServer` 静默复用端口上的未知进程；端口被占用时应明确失败并更换端口，不能把其他本机服务的响应误判为上游协议缺陷。
- 宝塔只是受支持的部署拓扑之一；宝塔专用的宿主机网络、反向代理层数和数据库地址默认值只能放在宝塔 Compose、安装向导或部署文档中，不得写入公共业务服务或全局默认配置。每次相关改动必须同时确认本机开发、默认 Docker、外部/云 PostgreSQL、文件/PostgreSQL Provider 以及本地/S3 存储的既有行为不变。
- 当前 Canvas、“我的素材”、短剧、工作台记录和 Agent 对话均由 PostgreSQL/JSON Provider 服务端保存；文档和实现不得重新引入浏览器业务缓存兜底。
- 外部存储开关只决定新媒体的写入位置：开启时新上传和新生成的图片、视频、音频只写 S3 兼容对象存储，不保留本地持久副本；关闭时新媒体只写服务器本地目录。历史媒体必须按登记的 `storage_provider` 读取，不能因当前开关变化而切换位置；业务记录只保存稳定站内 `storageKey` 和媒体 API URL，不保存对象 Key 或临时签名 URL。
- 外部存储 WebP 预览对象 Key 必须只由原始对象 Key 与规范化预览宽度决定；同一源图同一规格在所有页面复用同一个底层对象，允许每次读取生成新的短期签名但不得生成新的预览对象。`.vozeb-preview/` 对象禁止再次作为转换输入、业务媒体或管理员列表原件。
- 视频时长必须在请求上游前按最终模型的 `durationRange` 与能力档案归一化：离散档位选择不小于用户请求的最小档位，超过最大档位时使用最大值；连续范围按上下限收敛；未指定时默认 5 秒。任务必须持久化实际发送给上游的时长；上游成功后直接保存原始结果，不再用 FFprobe/FFmpeg 二次验时或转码，也不得因上游成片存在轻微时长差异把任务判为失败。
- Agent 子任务一旦发起过上游创建请求，超时、断流、无效 JSON、HTTP 5xx、查询异常、本地等待结束、`needs_review` 或 `submission_outcome_unknown` 都不得自动再次创建同类上游任务；必须保留原任务并继续由页面/Worker 查询或等待人工检查。只有图片上游明确返回生成终态失败时，系统才可退款并清除旧上游身份后自动重新提交 1 次，第二次明确失败必须终结；其他媒体仍只在用户显式点击重试后发起新的上游请求。
- 生成任务不得仅因 `updatedAt` 过旧或本地执行器心跳中断就把 `pending/running` 改为失败；读取中的页面可以低成本唤醒同一持久任务，常驻 Worker 通过租约接管无人页面任务，但两者都必须沿用已保存的上游任务 ID。`result_ready` 只重试媒体保存，任何自动恢复路径都禁止再次创建上游任务。
- 系统渠道代理、文本/图片/视频/音频生成入口、任务详情恢复、Agent Run/SSE 与生成 Worker Route 的静态 `maxDuration` 必须覆盖最长模型请求超时并预留结果落盘时间；内部 App 请求、全局代理和安全出站 Undici Dispatcher 的 `headersTimeout/bodyTimeout` 也必须覆盖同一窗口。不能只放宽模型 `AbortSignal` 而遗漏 Route 或传输层的默认 300 秒超时，调用方仍按文本、图片、视频和音频策略主动终止真实业务请求。
- 普通“生成图片/短视频”请求只能在当前对话返回媒体；只有用户原文同时明确项目目标与创建动作时，才允许创建 Canvas 或短剧项目。
- AI API Key 只保存在服务端系统渠道或服务器环境变量中；普通用户配置 Store 不得持久化 API Key，也不得由前端直接请求外部模型地址。
- 渠道模型目录路径必须限制为 Base URL 同源：先对 Base URL 执行出站安全校验，再从同一 origin 构造 `/v1/models` 等目录和分页地址；不得把合法同源路径重复误判为内网或其他域名，也不得向跨域目录或分页地址发送渠道密钥。
- 后台模型渠道不提供探测、批量检测、能力验证、验证记录或检测快照，也不得用检测结果限制渠道启用；管理员只在用户端文本、图片、视频和音频工作台发起真实业务请求验收。运行时自动故障切换、短期冷却和生成运维只读健康统计继续保留，不能混同为后台渠道测试功能。
- GitHub 公开 Skill 导入不得强制仓库声明 SPDX 或其他许可证；许可证存在时只作为来源元数据展示。导入仍必须固定到明确 commit、保存内容哈希、校验 GitHub 来源、默认停用且不得执行仓库代码。
- Linux Standalone 与发布镜像必须携带当前 CPU/libc 对应的 Sharp 原生包和 libvips 运行库，并在独立产物或镜像内验证可加载；Buildx 多架构 manifest 在 `pipefail` 下提取 digest 时必须完整消费 `imagetools inspect` 输出，禁止用会提前关闭管道的 `awk ... exit`。
- `chatgpt2api` 按标准 OpenAI 兼容渠道接入，禁止新增专用协议枚举、适配器或猜测路径；图片生成继续使用 OpenAI `/v1/images/generations` 与 `data[].url` / `b64_json` 契约。只有渠道显式声明 `queryPath` 或响应返回明确轮询地址时才允许异步查询；仅返回任务 ID 的非标准响应进入 `needs_review`，不得自动拼接查询路径、切换渠道、退款或重新创建任务。
- 逻辑模型目录必须按去除 `models/` 前缀且忽略大小写的上游模型名自动同步：同名模型跨渠道合并，不同名模型自动独立；保留已有稳定逻辑 ID、绑定启停、优先级、权重和能力档案。后台不得恢复手填逻辑模型 ID、名称、渠道或上游模型绑定；删除渠道必须等待服务端持久化成功后再关闭界面，并由服务端同步清理失效绑定和默认模型。
- 模型基础积分的稳定配置键是逻辑模型 ID；上游模型名只允许作为已有配置的兼容读取来源，并按启用绑定优先级解析。后台展示与编辑、前端预计积分、服务端实际扣费和退款必须复用同一价格解析规则，禁止分别按逻辑模型和上游别名查价。单价为 `0` 的免费调用仍必须写入幂等流水、累计套餐调用次数，并在上游失败时按原流水撤销用量。
- 服务器内部文本任务、Agent 规划、结构化分析和创作复盘调用系统渠道时，必须显式携带逻辑文本模型 ID；可由重放、恢复或重复提交触发的调用还必须使用包含用户、业务请求、候选渠道和上游模型身份的稳定幂等键。失败退款以消费流水 ID 和已解析的 `pointsCost` 是否存在为准，不能使用 `pointsCost > 0` 判断；0 积分调用也要撤销套餐文本次数。
- 普通用户提交的 `Idempotency-Key`、`X-Client-Request-Id` 或项目内部计费幂等头都不得直接成为本地消费流水身份；计费键必须由服务端绑定用户、业务请求、逻辑模型、渠道、上游模型和调用类型生成，并保存请求指纹。同一服务端业务身份对应不同正文或参数时必须返回 409，不能重复扣费或静默复用旧流水。
- Docker 静态资源路径目前仍是待办项，文档中不要过度承诺生产部署已经完全验证。

## Mandatory Testing

- After every code change, run the relevant automated checks and browser regression flow before reporting completion.
- 协议回归默认必须使用仓库内本地 TCP fixture、每次运行确认空闲的随机端口和固定测试凭据，禁止读取或调用管理员后台已配置的真实渠道/API；只有用户明确要求真实上游验收时才允许调用，并必须单独报告测试范围。
- Cover desktop and mobile layouts, canvas interactions including node actions and linking, image workbench generation/history/reference-image flows, video workbench text-to-video and image-to-video flows, all visible buttons touched by the change, and configured API capability checks for text/image/video.
- For live upstream API tests that include Chinese prompts, do not put Chinese literals directly in PowerShell commands. Use the app flow, Node/fetch with UTF-8 text loaded from a file, or base64/Unicode reconstruction before sending so the upstream prompt is not submitted as question marks.
- If the full live API/browser matrix cannot be completed, report the exact gap and why.
