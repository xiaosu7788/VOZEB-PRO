import type { AdminHelpArticleId } from "./admin-help-content";

export type AdminHelpTroubleshooting = {
    symptom: string;
    cause: string;
    actions: string[];
    caution?: string;
};

export type AdminHelpGuidance = {
    stepActions: string[][];
    troubleshooting: AdminHelpTroubleshooting[];
};

export const adminHelpGuidance: Record<AdminHelpArticleId, AdminHelpGuidance> = {
    "getting-started": {
        stepActions: [
            ["在安装向导先检测 PostgreSQL 地址、端口、数据库名和账号权限，检测通过后再初始化表结构。", "创建首个管理员后退出并重新登录一次，确认鉴权、会话和后台路由都能正常恢复。"],
            ["在站点资料上传无限进化 Logo，并检查登录页、用户端、后台、favicon 和助手头像没有三角形品牌回退。", "在模型渠道添加可用文本渠道，拉取模型并自动同步逻辑模型后设为默认，再执行一次真实文本检测。"],
            ["先创建一个内部测试商品，明确售价、积分或套餐权益和有效期，再核对对应逻辑模型的基础积分。", "只启用一个已完成商户资质、密钥和 HTTPS 回调验证的支付渠道，用小额测试订单走完支付与权益到账。"],
            ["选择本地或 S3 后上传一张图片和一个短视频，刷新页面确认仍可读取，并记录各自的 storage_provider。", "分别保存 PostgreSQL、媒体目录或 Bucket、环境变量和加密密钥，在隔离环境完成一次恢复验证。"],
        ],
        troubleshooting: [
            {
                symptom: "代码已经修改，但 3000 打开的仍是旧页面或仍跳 GitHub",
                cause: "3000 仍运行旧的 next start 生产包，或反向代理仍指向旧容器、旧端口和旧静态目录。",
                actions: ["检查 3000 的监听进程和启动命令，开发时使用 npm run dev。", "生产模式重新构建并重启当前实例，再确认代理目标和浏览器缓存。"],
                caution: "只改源码不会让已经运行的生产包自动更新；不要同时让多个项目实例争用同一端口。",
            },
            {
                symptom: "管理员可以登录，但 Agent、图片或视频一调用就失败",
                cause: "默认逻辑模型未设置、没有启用绑定，或上游 Base URL、API Key、协议和模型类型不匹配。",
                actions: ["先测试默认文本逻辑模型，再分别测试图片和视频能力。", "到调用记录核对逻辑模型、真实上游模型、请求地址和首个错误。"],
            },
            {
                symptom: "支付页面可下单，但支付成功后没有积分或套餐",
                cause: "支付回调未到达、验签失败、订单号不匹配，或回调处理没有完成幂等权益发放。",
                actions: ["核对公网 HTTPS 回调地址、商户密钥和渠道事件记录。", "在订单与财务流水确认回调状态后再决定补偿，不要先直接改余额。"],
            },
        ],
    },
    operations: {
        stepActions: [
            ["固定查看近 24 小时和近 7 日两个范围，并与上一周期比较用户、收入、请求量和成功率。", "发现峰值或断崖后记录具体时间，再按模型、任务类型和入口继续下钻。"],
            ["搜索目标用户，先核对账号状态和角色，再查看套餐有效期、剩余次数、积分余额及近期流水。", "人工调整权益时填写业务原因，并在调整后用新的普通会话验证权限是否即时生效。"],
            ["用用户、时间和任务类型缩小记录范围，打开单次调用查看逻辑模型、上游模型、任务 ID、耗时和错误。", "把创建失败、轮询失败、超时和上游终态失败分开处理，并核对对应消费流水是否撤销。"],
            ["从异常任务进入关联会话、项目和媒体，确认前端展示状态与服务端任务终态一致。", "需要重试时让用户显式触发，先清理旧子任务状态，再创建新的上游请求。"],
        ],
        troubleshooting: [
            {
                symptom: "经营看板显示为 0，但实际存在用户、订单或任务",
                cause: "统计时间范围、数据库 Provider、时区或聚合查询与当前运行实例不一致。",
                actions: ["确认页面时间范围和服务器时区。", "用订单、用户或调用记录的原始列表验证数据源，再检查当前实例连接的数据库。"],
            },
            {
                symptom: "同一生成请求出现多条上游任务或重复扣费",
                cause: "页面重复提交、恢复流程再次创建任务，或服务端缺少稳定幂等键。",
                actions: ["按用户、会话、Run 和上游任务 ID 对比重复记录。", "保留失败证据并停止自动重建，只有用户显式重试时发起新任务。"],
                caution: "不要删除重复记录来掩盖问题，先核对积分、套餐次数和上游成本。",
            },
            {
                symptom: "用户已被禁用，但仍能看到旧页面或旧任务继续运行",
                cause: "用户保留了旧会话，或禁用发生在任务已经提交之后。",
                actions: ["让用户重新请求受保护接口，确认服务端鉴权已拒绝。", "区分禁止新建任务和终止既有上游任务，按业务规则处理已产生的成本与结果。"],
            },
        ],
    },
    commerce: {
        stepActions: [
            ["创建商品时逐项填写名称、真实日常价、积分或套餐权益、有效期和排序，先保持停用完成内部检查。", "启用后分别在套餐页、结算页和订单预览核对名称、价格、权益和说明。"],
            ["选择目标商品，设置活动开始与结束时间、活动标签和活动价，保存前检查同商品是否已有重叠活动。", "在活动前、活动中和活动后分别验证售价；只有活动价低于真实日常价时显示删除线。"],
            ["先确定固定金额或比例优惠，再配置门槛、适用商品、有效期、库存和每用户限领次数。", "用符合条件、不符合条件和已达上限的三个测试账号验证领取、使用、锁定和释放。"],
            ["先按获客成本设置邀请人和新用户奖励，再配置首单最低实付、冷静期、月度/活动上限与自动冻结策略。", "启用后用多个测试账号验证邀请访问、注册绑定、首单触发、到期结算、退款撤销和人工恢复，确认维护调度已配置。"],
            ["打开订单详情核对日常价、活动优惠、优惠券优惠、实付和权益快照。", "关闭、人工确认或退款前先核对支付渠道状态；处理后再检查优惠券、积分、套餐和流水。"],
        ],
        troubleshooting: [
            {
                symptom: "前台没有显示折扣删除线或活动价",
                cause: "活动未启用、时间未生效、活动价不低于日常价，或当前商品不在活动范围。",
                actions: ["核对服务器时间、活动起止时间和商品绑定。", "对比商品日常价与当前成交价，刷新套餐页和结算页确认。"],
                caution: "不要为显示删除线临时抬高日常价，原价必须是长期真实可售价格。",
            },
            {
                symptom: "用户明明领取了优惠券，结算时却不可用",
                cause: "未达到门槛、商品不适用、已过期、库存或用户次数耗尽，或优惠券被另一笔待支付订单锁定。",
                actions: ["在优惠券记录核对状态、适用范围和锁定订单。", "取消或等待旧订单超时后确认锁定是否释放，再重新结算。"],
            },
            {
                symptom: "退款后金额、优惠券和用户权益对不上",
                cause: "只更新了订单状态，支付退款、权益撤销或优惠券状态尚未完成。",
                actions: ["以订单创建时保存的价格和优惠快照为准核对。", "依次检查支付退款流水、积分或套餐撤销和优惠券退款规则。"],
            },
            {
                symptom: "受邀用户已经支付，但奖励没有发放",
                cause: "订单不是首笔实付、金额未达门槛、仍在冷静期、关系被风险冻结、奖励名额已达上限，或维护结算任务尚未执行。",
                actions: ["在邀请关系和奖励记录核对触发订单、风险状态、结算时间与拒绝原因。", "确认订单终态和维护调度后再使用立即结算；不要通过手工加积分绕过奖励账本。"],
                caution: "恢复冻结关系前必须记录复核依据；已锁定或已核销的奖励优惠券发生退款时需要人工处理，不能伪造撤销成功。",
            },
        ],
    },
    finance: {
        stepActions: [
            ["先按逻辑模型 ID 配置基础积分，再逐项配置数量、时长、清晰度等参数倍率。", "用免费模型、普通模型和高倍率参数各执行一次，比较前端预计、消费流水和失败撤销。"],
            ["每个支付渠道分别填写服务端密钥、证书、回调地址和必要的环境变量，保存后先运行配置检测。", "使用测试或小额订单验证创建、支付、重复回调、乱序回调和退款，不要只验证跳转页面。"],
            ["按活动批次创建 CDK，记录用途、权益、数量、有效期和负责人，生成后立即受控导出明文。", "用未使用、已使用、过期和停用四种状态验证兑换结果与积分或套餐流水。"],
            ["每天按渠道对比成功订单、实收、退款和手续费，再核对积分余额、套餐次数和人工调整。", "发现差异时记录订单号、渠道流水号和消费流水 ID，先冻结重复处理再定位。"],
        ],
        troubleshooting: [
            {
                symptom: "前端预计积分与实际扣费不一致",
                cause: "前端按逻辑模型计价，服务端却按上游别名计价，或参数倍率未使用同一解析规则。",
                actions: ["在调用记录确认逻辑模型 ID、上游模型和已解析 pointsCost。", "统一从逻辑模型价格规则读取，免费调用也检查套餐次数和幂等流水。"],
            },
            {
                symptom: "支付渠道重复回调后用户获得了多份权益",
                cause: "回调事件或订单权益发放缺少幂等约束，重复事件被当作新支付处理。",
                actions: ["按渠道事件 ID、订单号和支付流水检查重复记录。", "暂停人工补发，先修复幂等状态，再对多发权益做有审计的冲正。"],
            },
            {
                symptom: "CDK 无法兑换或后台找不到明文",
                cause: "兑换码已过期、停用、达到次数限制，且系统只保存安全摘要，不长期保存明文。",
                actions: ["按批次、状态和备注检查兑换记录。", "明文遗失时重新生成新批次并停用旧批次，不要尝试从数据库还原。"],
            },
        ],
    },
    models: {
        stepActions: [
            ["在模型渠道按向导依次选择协议、配置连接、添加模型、同步逻辑模型和复核启用；有目录时拉取，没有公开目录时手动填写模型 ID。", "未知上游使用自定义协议助手导入文档或示例，复核请求路径、鉴权、模板和结果字段后，再到对应用户工作台发起真实请求。"],
            ["同步后确认同名上游模型已跨渠道合并、不同名模型各自独立，再维护能力类型、启停、绑定优先级和权重。", "逐个停用、启用绑定验证路由顺序，确认没有可用绑定时服务端会明确失败而不是静默换类型。"],
            ["先设置并验证默认文本模型，因为后台规划、Agent、Canvas 和内部分析都依赖它。", "再设置图片、视频和音频默认能力，用普通用户身份验证页面选择与实际路由一致。"],
            ["为 Skill 写清触发范围、输入限制和输出目标，并绑定真实已启用的逻辑能力。", "用相似需求和无关需求各测试一次，确认不会误触发，也不会在用户不可用时显示可执行状态。"],
        ],
        troubleshooting: [
            {
                symptom: "工作台真实调用返回 404、路径不存在或协议错误",
                cause: "Base URL 已包含 /v1 但请求模板再次追加，或所选协议与供应商接口格式不一致。",
                actions: ["对照供应商文档检查基础地址和最终请求路径。", "切换到项目支持的正确协议后重新拉取模型，不要通过连续拼接路径试错。"],
            },
            {
                symptom: "可以拉取模型列表，但真实调用仍失败",
                cause: "账号没有该模型权限、模型类型选择错误、绑定未启用，或模型只支持部分参数。",
                actions: ["在对应用户工作台对目标模型发出真实业务请求。", "到调用记录读取上游状态码和响应摘要，再调整绑定和参数。"],
            },
            {
                symptom: "Agent 文本正常，但图片或视频始终不可用",
                cause: "只验证了默认文本模型，图片或视频逻辑模型没有可用绑定、默认值或对应计费规则。",
                actions: ["在创作 Agent 中分别发起图片和视频真实请求。", "确认逻辑类型、默认模型、上游绑定和积分规则四处使用同一逻辑模型 ID。"],
            },
        ],
    },
    system: {
        stepActions: [
            ["在站点资料上传无限进化 Logo，保存后依次检查首页、登录、用户工作区、后台、助手头像和浏览器图标。", "同时检查浅色、深色、favicon、manifest 和 metadata 回退，确保任何默认状态都不出现三角形品牌标识。"],
            ["先保存 SMTP 主机、端口、SSL、账号、授权码和发件人并发送真实测试邮件。", "邮件验证通过后再开启注册或找回流程；生成默认参数则按当前默认逻辑模型支持范围设置。"],
            ["打开注销申请核对用户身份、申请时间、公开内容、媒体、订单和审计保留要求。", "记录受理或拒绝理由，执行后用普通接口验证账号状态、公开内容和媒体权限。"],
            ["在更新中心确认当前版本、更新日志和升级要求，并先完成数据库与媒体备份。", "升级后检查安装状态、登录、生成、支付、存储和后台文档，再决定是否开放流量。"],
        ],
        troubleshooting: [
            {
                symptom: "已经上传无限进化 Logo，但部分页面仍显示旧图标或三角形",
                cause: "后台配置、/logo.svg、/icon.svg、favicon、manifest、metadata 或助手头像仍有独立硬编码和浏览器缓存。",
                actions: ["逐项检查品牌来源和网络请求实际地址。", "清理旧构建并重启，硬刷新浏览器后再验证浅色、深色和未登录状态。"],
                caution: "播放、告警等功能性三角图标可以保留，但不得作为品牌 Logo、默认占位或回退。",
            },
            {
                symptom: "SMTP 测试成功，但用户收不到验证码或找回邮件",
                cause: "业务开关未启用、发件人与认证账号不一致、邮件进入垃圾箱，或发送频率限制触发。",
                actions: ["检查注册策略、邮件任务错误和目标邮箱垃圾箱。", "核对 From、域名解析和 SMTP 服务商限制，再用不同邮箱复测。"],
            },
            {
                symptom: "浅色正常，切换深色后文字、按钮或图标看不清",
                cause: "组件只设置了背景，没有显式设置深色文字、边框、hover 或图标颜色。",
                actions: ["检查组件所有可点击态和 Ant Design Portal 弹层。", "为浅色、深色、hover、active 和 disabled 分别提供可读对比。"],
            },
        ],
    },
    storage: {
        stepActions: [
            ["按用户、来源、类型和时间筛选本地媒体，打开详情确认原文件名、归属和业务关联。", "删除前查看引用检查结果；存在会话、素材、Canvas、短剧或任务引用时先处理业务关系。"],
            ["填写 Endpoint、Region、Bucket、访问凭据、对象前缀和 path-style，使用最小权限账号运行连接测试。", "启用后上传新媒体，确认数据库登记为外部 Provider，站内读取使用短期签名 URL。"],
            ["先选择少量历史媒体迁移，核对对象上传、登记、站内读取和原业务页面。", "确认目标对象稳定可读后再按批次迁移，并保留失败清单以便幂等重试。"],
            ["制定 PostgreSQL、媒体、配置和加密密钥的同批次备份计划，记录版本和创建人。", "在独立环境按文档恢复，抽查用户、任务、本地媒体和外部对象，记录恢复耗时与缺口。"],
        ],
        troubleshooting: [
            {
                symptom: "切换外部存储后，历史图片或视频突然无法打开",
                cause: "读取逻辑错误地跟随当前存储开关，而没有按每条媒体登记的 storage_provider 读取。",
                actions: ["查看失效媒体的 Provider、storageKey 和登记记录。", "恢复按历史 Provider 读取；存储开关只影响之后的新写入。"],
            },
            {
                symptom: "S3 连接或上传成功，但用户页面仍无法显示媒体",
                cause: "签名地址、Endpoint 公私网选择、服务器时间、对象权限或响应 Content-Type 不正确。",
                actions: ["从站内媒体 API 获取一次新签名 URL 并检查状态码。", "核对服务器时间、对象 Key、签名有效期和最小读取权限。"],
            },
            {
                symptom: "后台提示媒体被引用，无法删除",
                cause: "文件仍关联创作会话、素材库、Canvas、短剧或生成记录，引用保护正在生效。",
                actions: ["根据引用列表进入对应业务记录确认是否可解除。", "优先下线或删除业务实体，再重新执行媒体删除。"],
                caution: "不要使用 force 绕过引用保护，否则会留下能看到记录但打不开文件的数据事故。",
            },
        ],
    },
    content: {
        stepActions: [
            ["先确定公告目标和受众，用简短标题、明确正文和必要链接创建草稿，再检查桌面与手机预览。", "只有故障、规则或重要活动需要时才开启首页或登录后弹窗，保存后用普通用户验证。"],
            ["为公告记录开始、结束或下线条件，定期检查置顶和弹窗内容是否仍有效。", "事件结束后及时停用；需要修正文案时优先更新同一公告，避免重复触达。"],
            ["为公共提示词填写可搜索标题、完整正文、分类、标签和预览，先在对应真实模型执行。", "确认不包含 API Key、内部规划提示词和用户隐私后再启用，并定期下线失效内容。"],
        ],
        troubleshooting: [
            {
                symptom: "用户每次登录都反复看到同一条公告",
                cause: "公告持续启用登录后弹窗，或用户确认状态、公告版本和身份关联没有正确保存。",
                actions: ["确认公告是否仍需要强制触达。", "检查用户确认记录和公告版本；过期内容直接停用，不通过叠加新公告修复。"],
            },
            {
                symptom: "后台提示词测试正常，但用户端找不到或执行失败",
                cause: "提示词未启用、分类筛选不匹配，或示例使用了用户端没有启用的模型能力。",
                actions: ["用普通用户检查启用状态、分类和搜索关键词。", "在用户实际可选的逻辑模型上重新执行，并修正文案和预览。"],
            },
            {
                symptom: "公告或提示词在手机端出现横向滚动和按钮被截断",
                cause: "正文含超长 URL、固定宽度、桌面卡片内边距或未换行文本。",
                actions: ["在 390px 和 430px 检查页面与内部滚动容器。", "让长链接和正文可换行，手机端使用紧凑间距并缩短操作文案。"],
            },
        ],
    },
    maintenance: {
        stepActions: [
            ["记录故障开始时间、受影响用户、入口和能力类型，再用一个管理员与一个普通用户复现。", "先检查登录和静态页面，把全站故障、单能力故障、单渠道故障和单任务故障分开。"],
            ["从生成运维按时间找到异常任务，再进入调用记录查看逻辑模型、上游模型、任务 ID、状态和首个错误。", "执行渠道检测并核对同一子任务是否只创建一次；不要在原因不明时反复切换模型和重试。"],
            ["生成问题核对消费与退款流水，支付问题核对订单和渠道事件，媒体问题核对 Provider、storageKey 和真实对象。", "发现状态不一致时先导出证据和备份，再使用业务入口修正，避免直接改数据库。"],
            ["恢复后按登录、文本、图片、视频、支付、媒体和历史记录顺序回归桌面与手机。", "记录根因、影响数据、修复和预防措施；能自动检测的问题增加测试、告警或巡检项。"],
        ],
        troubleshooting: [
            {
                symptom: "更换模型或渠道后，所有新生成任务同时失败",
                cause: "默认逻辑模型、启用绑定、上游凭据或请求协议被改坏。",
                actions: ["暂停继续修改，保留首个失败请求。", "依次检查默认逻辑模型、绑定优先级、渠道检测和调用记录，不要同时改多个变量。"],
            },
            {
                symptom: "生成成功，但新媒体或历史媒体无法显示",
                cause: "媒体登记、storage_provider、文件写入、对象签名或站内媒体 API 链路异常。",
                actions: ["分别测试一条新媒体和一条历史媒体。", "核对数据库登记、真实文件或对象和媒体 API 返回，确认问题位于写入还是读取。"],
            },
            {
                symptom: "订单、积分余额、套餐次数或退款统计彼此不一致",
                cause: "支付回调延迟或重复、失败退款遗漏、人工调整无记录，或统计口径时间范围不同。",
                actions: ["按单个订单或消费流水 ID 建立时间线。", "对比订单、支付、积分和套餐流水后再冲正，并记录原因和责任人。"],
            },
            {
                symptom: "部署或代码更新后页面没有变化",
                cause: "仍在运行旧生产包、旧容器或旧反向代理目标，浏览器打开的不是刚修改的实例。",
                actions: ["检查访问域名最终代理到的主机、端口和进程启动命令。", "开发环境使用 npm run dev；生产环境重新构建、重启并核对版本信息。"],
            },
        ],
    },
};
